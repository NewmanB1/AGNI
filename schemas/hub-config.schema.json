{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://agni-project.org/schemas/hub-config.schema.json",
  "title": "AGNI Hub Configuration",
  "description": "Per-hub configuration for ports, directories, and hub identity. All properties are optional; defaults are resolved by env-config.js at runtime.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "embeddingDim": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1024,
      "description": "Embedding dimension for LMS engine (featureDim = embeddingDim * 2). Must match across federating hubs. Default 16 if omitted."
    },
    "forgetting": {
      "type": "number",
      "minimum": 0.5,
      "maximum": 1,
      "description": "Exponential decay for bandit/embedding (0.5–1). Values outside [0.5,1] corrupt decay; engine rejects at runtime. Default 0.98 if omitted."
    },
    "maxStudents": {
      "type": "integer",
      "minimum": 0,
      "description": "Cap on students. 0 = no limit. Engine and accounts reject when at cap. For Pi budget use 60."
    },
    "maxLessons": {
      "type": "integer",
      "minimum": 0,
      "description": "Cap on lessons. 0 = no limit. Engine rejects when at cap. For Pi budget use 200."
    },
    "thetaPort": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "TCP port for the theta (adaptive engine) server."
    },
    "servePort": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "TCP port for the static file / lesson-serve server."
    },
    "sentryPort": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "TCP port for the sentry (monitoring) server."
    },
    "dataDir": {
      "type": "string",
      "description": "Absolute path to the data directory (JSON stores, telemetry, etc.)."
    },
    "serveDir": {
      "type": "string",
      "description": "Absolute path to the compiled lesson-serve output directory."
    },
    "yamlDir": {
      "type": "string",
      "description": "Absolute path to the source YAML lesson directory."
    },
    "factoryDir": {
      "type": "string",
      "description": "Absolute path to the SVG factory output directory."
    },
    "katexDir": {
      "type": "string",
      "description": "Absolute path to the KaTeX CSS/font asset directory."
    },
    "hubId": {
      "type": "string",
      "description": "Human-readable identifier for this hub instance (e.g. 'village-pi-01')."
    }
  }
}
